# =============================================================================
# Environment Variables Template for Motivise Backend
# =============================================================================
# Copy this file to .env and update with your actual values
# NEVER commit .env to version control!

# -----------------------------------------------------------------------------
# JWT Configuration (CRITICAL - MUST BE SECURE)
# -----------------------------------------------------------------------------
# Generate a strong secret key (min 256 bits / 32 characters)
# Use: openssl rand -base64 32
# Or online: https://www.grc.com/passwords.htm
JWT_SECRET_KEY=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_KEY_MIN_32_CHARS

# JWT token expiration in milliseconds
# 3600000 = 1 hour
# 86400000 = 24 hours
JWT_EXPIRATION_MS=3600000

# -----------------------------------------------------------------------------
# Database Configuration (Production)
# -----------------------------------------------------------------------------
DB_URL=jdbc:mysql://localhost:3306/webbackend
DB_USERNAME=webbackenduser
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD

# -----------------------------------------------------------------------------
# MinIO File Storage (Production)
# -----------------------------------------------------------------------------
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=CHANGE_THIS_MINIO_SECRET
MINIO_BUCKET_NAME=motivise-uploads

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
SERVER_PORT=8081
SPRING_PROFILES_ACTIVE=docker-free

# -----------------------------------------------------------------------------
# CORS Configuration (Production)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed frontend origins
# Example: CORS_ALLOWED_ORIGINS=https://motivise.app,https://www.motivise.app
CORS_ALLOWED_ORIGINS=https://motivise.app

# -----------------------------------------------------------------------------
# Security Notes
# -----------------------------------------------------------------------------
# 1. JWT_SECRET_KEY must be at least 256 bits (32 characters) for HMAC-SHA256
# 2. Never use default values in production
# 3. Rotate secrets periodically
# 4. Use different secrets for dev/staging/production environments
