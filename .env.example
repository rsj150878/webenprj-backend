# =============================================================================
# Motivise Backend - Environment Variables Template
# =============================================================================
# ⚠️ THIS FILE IS NOT LOADED BY THE APPLICATION ⚠️
# 
# Spring Boot does not automatically load .env files.
# This file documents the environment variables that must be set at the OS level.
#
# How to use:
# - Development (quick-start.cmd): No env vars needed, uses hardcoded defaults
# - Development (start.cmd): Set these as OS environment variables
# - Production: Set these in your deployment environment (Docker, K8s, cloud)
#
# DO NOT commit actual secrets to version control

# -----------------------------------------------------------------------------
# JWT Configuration (REQUIRED for start.cmd and production)
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
# For quick-start.cmd: Not needed (uses hardcoded dev secret)
# For start.cmd: Set as OS env var before running
# For production: MUST be set in deployment environment
JWT_SECRET_KEY=AhCJ4lNWxhr+bha4JNNtECAXvE41JLADJ5AqydXhBew=

# JWT token expiration (1 hour)
JWT_EXPIRATION_MS=3600000

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED for start.cmd and production)
# -----------------------------------------------------------------------------
# For quick-start.cmd: Not needed (uses H2 in-memory)
# For start.cmd: Uses values from docker-compose.yml
# For production: Set these in deployment environment
# For docker-free profile, H2 in-memory is used (no env vars needed)
DB_URL=jdbc:mysql://localhost:3306/webbackend
DB_USERNAME=webbackenduser
DB_PASSWORD=webbackendpassword

# -----------------------------------------------------------------------------
# MinIO Configuration (Optional - uses defaults if not set)
# -----------------------------------------------------------------------------
# For quick-start.cmd: Not needed (mock file storage)
# For start.cmd: Optional (defaults to docker-compose values)
# For production: Set these in deployment environment
MINIO_URL=http://localhost
MINIO_PORT=9000
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
SERVER_PORT=8081
SPRING_PROFILES_ACTIVE=docker-free
